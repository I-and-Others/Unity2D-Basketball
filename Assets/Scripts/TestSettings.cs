using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestSettings : MonoBehaviour
{

    public static float speedValue;

   /* void Start(){
    	LoadSettings();
    	Debug.Log("Başlangıçtaki Speed value = " + speedValue);
    }
    void LateUpdate(){
    	Debug.Log("Speed value = " + speedValue);
    }
    public void SaveSettings(){
		SaveSystem.SaveData(this);
	}

	public void LoadSettings(){
		GameData data = SaveSystem.LoadSettings();
		speedValue = data.speedValue;
	}

    */
}
